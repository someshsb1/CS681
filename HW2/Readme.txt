Calculated the vaccination rate for each age category (Old, Mid, Young)

By using Collectors.groupingBy().

Calculated the average # of vaccinations administered in each age category.
By using Collectors.groupingBy().

Calculated the average age of the people who have never been vaccinated.
By using Collectors.partitioningBy().

For each calculation, created 1,100 instances of Person randomly.